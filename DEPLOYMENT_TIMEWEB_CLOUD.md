# Инструкция по развертыванию PayPlanner на Timeweb Cloud

## Оглавление
1. [Предварительные требования](#предварительные-требования)
2. [Обнаруженные проблемы и их решения](#обнаруженные-проблемы)
3. [Подготовка инфраструктуры](#подготовка-инфраструктуры)
4. [Развертывание Backend](#развертывание-backend)
5. [Развертывание Frontend](#развертывание-frontend)
6. [Настройка домена и SSL](#настройка-домена-и-ssl)
7. [Проверка работы](#проверка-работы)

---

## Предварительные требования

- Аккаунт в Timeweb Cloud
- Доменное имя (для Telegram Mini App обязательно нужен HTTPS)
- Telegram Bot Token от [@BotFather](https://t.me/BotFather)
- Настроенные облачные базы данных PostgreSQL и Redis в Timeweb Cloud

---

## Обнаруженные проблемы

### 🔴 Критические проблемы, требующие исправления:

#### 1. **Redis конфигурация не поддерживает пароль**
   - **Файл**: `PayPlannerBackend/src/config/redis.ts`
   - **Проблема**: Облачный Redis в Timeweb Cloud требует аутентификацию
   - **Решение**: Добавить поддержку пароля в конфигурацию Redis

#### 2. **Несоответствие переменных окружения Frontend**
   - **Проблема**: В README указано `REACT_APP_API_URL`, но код использует `VITE_API_URL`
   - **Файл**: `PayPlannerFrontend/src/store/api.ts`
   - **Решение**: Исправить README или использовать правильную переменную

#### 3. **Миграции БД нужно запускать вручную**
   - **Проблема**: Dockerfile не запускает миграции автоматически
   - **Решение**: Добавить выполнение миграций при старте контейнера или запускать вручную

### ⚠️ Потенциальные проблемы:

1. **CORS настройки**: Нужно добавить продакшен домен в `ALLOWED_ORIGINS`
2. **SSL сертификаты**: Telegram Mini App требует HTTPS
3. **Порты**: Убедиться, что порты 3000 (backend) и 3001 (frontend) доступны
4. **Health check**: Проверить работу endpoint `/health` для балансировщика

---

## Подготовка инфраструктуры

### Шаг 1: Создание облачных баз данных

1. **PostgreSQL**:
   - В панели Timeweb Cloud → **Облачные базы данных**
   - Создайте новую PostgreSQL базу данных
   - Запишите данные подключения:
     - Хост (host)
     - Порт (обычно 5432)
     - Имя базы данных
     - Пользователь
     - Пароль

2. **Redis**:
   - В панели Timeweb Cloud → **Облачные базы данных**
   - Создайте новую Redis базу данных
   - Запишите данные подключения:
     - Хост (host)
     - Порт (обычно 6379)
     - Пароль (если требуется)

### Шаг 2: Создание облачных серверов

#### Backend сервер:
1. Создайте облачный сервер (VDS/VPS) для Backend
   - Минимальные требования:
     - 1 CPU
     - 1 GB RAM
     - 20 GB SSD
   - Операционная система: Ubuntu 22.04 или Debian 12
   - Установите Docker и Docker Compose (если нужно)

#### Frontend сервер:
1. Создайте облачный сервер или используйте статический хостинг
   - Для статики можно использовать CDN или S3-хранилище Timeweb Cloud
   - Либо отдельный сервер с Nginx

### Шаг 3: Создание балансировщика нагрузки (опционально)

- Если планируется высокая нагрузка, создайте балансировщик нагрузки в Timeweb Cloud
- Настройте правила маршрутизации на порт 3000 (backend)

---

## Развертывание Backend

### Шаг 1: Подготовка сервера

```bash
# Подключитесь к серверу по SSH
ssh root@your-backend-server-ip

# Установите Docker (если не установлен)
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# Установите Docker Compose (если нужно)
apt-get update
apt-get install docker-compose-plugin
```

### Шаг 2: Исправление проблем в коде

#### Исправление 1: Добавление поддержки пароля Redis

Создайте файл `PayPlannerBackend/src/config/redis.ts` с поддержкой пароля:

```typescript
import Redis from 'ioredis';
import dotenv from 'dotenv';

dotenv.config();

export const redis = new Redis({
  host: process.env.REDIS_HOST || 'localhost',
  port: parseInt(process.env.REDIS_PORT || '6379', 10),
  password: process.env.REDIS_PASSWORD, // Добавить поддержку пароля
  retryStrategy: (times) => {
    const delay = Math.min(times * 50, 2000);
    return delay;
  },
});

redis.on('error', (err) => {
  console.error('Redis connection error:', err);
});

redis.on('connect', () => {
  console.log('Redis connected successfully');
});
```

#### Исправление 2: Добавление запуска миграций в Dockerfile

Обновите `PayPlannerBackend/Dockerfile`:

```dockerfile
# ... existing code ...

# Добавить скрипт для запуска миграций при старте
COPY --from=builder /app/src/db ./src/db
COPY --from=builder /app/node_modules ./node_modules

# Создать entrypoint скрипт
RUN echo '#!/bin/sh\n\
npm run migrate\n\
node dist/index.js' > /app/entrypoint.sh && \
chmod +x /app/entrypoint.sh

EXPOSE 3000

USER node

CMD ["/app/entrypoint.sh"]
```

**Альтернативный вариант** (рекомендуется): Запускать миграции отдельно через CI/CD или вручную:

```bash
# После деплоя выполнить
docker exec -it payplaner_backend npm run migrate
```

### Шаг 3: Клонирование репозитория

```bash
# На сервере
cd /opt
git clone https://github.com/Pailon/PayPlannerBackend.git
cd PayPlannerBackend
```

### Шаг 4: Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```bash
# Backend конфигурация
NODE_ENV=production
PORT=3000

# Telegram Bot
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_BOT_SECRET=your_bot_secret_here
WEB_APP_URL=https://your-frontend-domain.com

# PostgreSQL (из облачной БД Timeweb Cloud)
POSTGRES_HOST=your-postgres-host.timeweb.cloud
POSTGRES_PORT=5432
POSTGRES_USER=your_postgres_user
POSTGRES_PASSWORD=your_postgres_password
POSTGRES_DB=your_postgres_db

# Redis (из облачной БД Timeweb Cloud)
REDIS_HOST=your-redis-host.timeweb.cloud
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password

# JWT
JWT_SECRET=your_jwt_secret_min_32_chars
JWT_REFRESH_SECRET=your_jwt_refresh_secret

# Encryption
ENCRYPTION_KEY=your_64_char_hex_key

# CORS
ALLOWED_ORIGINS=https://web.telegram.org,https://web.telegram.org/k,https://your-frontend-domain.com
```

**Генерация секретов:**

```bash
# JWT_SECRET
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# ENCRYPTION_KEY
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

### Шаг 5: Сборка и запуск Docker контейнера

```bash
# Сборка образа
docker build -t payplaner-backend .

# Запуск контейнера
docker run -d \
  --name payplaner_backend \
  --restart unless-stopped \
  -p 3000:3000 \
  --env-file .env \
  payplaner-backend

# Или если используете docker-compose (нужно создать файл)
docker-compose up -d
```

### Шаг 6: Выполнение миграций

```bash
# Выполнить миграции БД
docker exec -it payplaner_backend npm run migrate
```

### Шаг 7: Проверка работы Backend

```bash
# Проверить логи
docker logs payplaner_backend

# Проверить health endpoint
curl http://localhost:3000/health
```

---

## Развертывание Frontend

### Вариант 1: Развертывание через Docker (рекомендуется)

#### Шаг 1: Подготовка сервера

```bash
# Подключитесь к серверу
ssh root@your-frontend-server-ip

# Установите Docker (если не установлен)
# (см. инструкции выше)
```

#### Шаг 2: Исправление README

В `PayPlannerFrontend/README.md` исправить переменную окружения:
- Было: `REACT_APP_API_URL`
- Должно быть: `VITE_API_URL`

#### Шаг 3: Клонирование репозитория

```bash
cd /opt
git clone https://github.com/Pailon/PayPlannerFrontend.git
cd PayPlannerFrontend
```

#### Шаг 4: Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```bash
# API URL (URL вашего Backend)
VITE_API_URL=https://your-backend-domain.com/api

# Telegram Bot Name (опционально)
VITE_TELEGRAM_BOT_NAME=your_bot_name
```

**Важно**: В Vite переменные окружения должны начинаться с `VITE_`

#### Шаг 5: Сборка и запуск

```bash
# Сборка образа
docker build -t payplaner-frontend .

# Запуск контейнера
docker run -d \
  --name payplaner_frontend \
  --restart unless-stopped \
  -p 3001:3001 \
  --env-file .env \
  payplaner-frontend
```

### Вариант 2: Развертывание статики через S3/CDN

Если хотите использовать статический хостинг:

```bash
# Локально или в CI/CD
npm install
npm run build

# Загрузить содержимое dist/ в S3 или на сервер
scp -r dist/* user@your-server:/var/www/html/
```

---

## Настройка домена и SSL

### Шаг 1: Настройка DNS

1. В панели Timeweb Cloud найдите ваш домен
2. Настройте A-записи:
   - `api.yourdomain.com` → IP адрес Backend сервера
   - `app.yourdomain.com` → IP адрес Frontend сервера (или CDN)

### Шаг 2: Получение SSL сертификата

1. **Через Timeweb Cloud**:
   - В панели управления → **SSL-сертификаты**
   - Закажите бесплатный Let's Encrypt сертификат
   - Назначьте сертификат на домены

2. **Или через Nginx с Let's Encrypt**:
   ```bash
   # Установите certbot
   apt-get install certbot python3-certbot-nginx

   # Получите сертификат
   certbot --nginx -d api.yourdomain.com
   certbot --nginx -d app.yourdomain.com
   ```

### Шаг 3: Настройка Nginx (опционально, для reverse proxy)

Если используете Nginx перед Docker контейнерами:

**Backend Nginx конфиг** (`/etc/nginx/sites-available/api.yourdomain.com`):

```nginx
server {
    listen 80;
    server_name api.yourdomain.com;
    
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

**Frontend Nginx конфиг** (`/etc/nginx/sites-available/app.yourdomain.com`):

```nginx
server {
    listen 80;
    server_name app.yourdomain.com;
    root /var/www/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /assets {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

После настройки:
```bash
# Активировать конфиги
ln -s /etc/nginx/sites-available/api.yourdomain.com /etc/nginx/sites-enabled/
ln -s /etc/nginx/sites-available/app.yourdomain.com /etc/nginx/sites-enabled/

# Проверить конфигурацию
nginx -t

# Перезапустить Nginx
systemctl restart nginx
```

---

## Проверка работы

### 1. Проверка Backend

```bash
# Health check
curl https://api.yourdomain.com/health

# Должен вернуть:
# {"status":"ok","timestamp":"2025-01-XX..."}
```

### 2. Проверка Frontend

- Откройте `https://app.yourdomain.com` в браузере
- Проверьте, что страница загружается

### 3. Проверка Telegram Bot

1. Откройте Telegram бота
2. Отправьте команду `/start`
3. Проверьте, что кнопка "Открыть приложение" работает
4. Убедитесь, что мини-приложение открывается по HTTPS

### 4. Проверка интеграции

1. Откройте мини-приложение в Telegram
2. Попробуйте авторизоваться
3. Создайте тестовую подписку
4. Проверьте работу всех функций

---

## Рекомендации по безопасности

1. **Firewall**: Настройте firewall в Timeweb Cloud:
   - Откройте только необходимые порты (80, 443, 22)
   - Закройте доступ к PostgreSQL и Redis из интернета (используйте приватную сеть)

2. **Переменные окружения**: Храните секреты в безопасном месте:
   - Используйте секреты в CI/CD
   - Не коммитьте `.env` файлы в git

3. **Мониторинг**: Настройте мониторинг:
   - Логи приложения
   - Мониторинг БД
   - Uptime monitoring

4. **Резервное копирование**: Настройте автоматические бэкапы:
   - PostgreSQL базы данных
   - Redis (если нужно)

---

## Устранение типичных проблем

### Backend не подключается к БД

- Проверьте правильность данных подключения в `.env`
- Убедитесь, что БД доступна из сервера (проверьте firewall)
- Проверьте логи: `docker logs payplaner_backend`

### Frontend не подключается к Backend

- Проверьте `VITE_API_URL` в `.env`
- Убедитесь, что CORS настроен правильно
- Проверьте, что Backend доступен по указанному URL

### Telegram Mini App не работает

- Убедитесь, что используется HTTPS (обязательно!)
- Проверьте, что домен добавлен в настройки бота (если требуется)
- Проверьте `WEB_APP_URL` в Backend конфигурации

### Ошибки при миграциях

- Убедитесь, что пользователь БД имеет права на создание таблиц
- Проверьте подключение к БД перед запуском миграций

---

## Полезные ссылки

- [Документация Timeweb Cloud](https://timeweb.cloud/docs)
- [Облачные базы данных Timeweb Cloud](https://timeweb.cloud/docs/cloud-databases)
- [Управление серверами](https://timeweb.cloud/docs/servers)
- [SSL сертификаты](https://timeweb.cloud/docs/ssl-certificates)

---

## Дополнительные улучшения

1. **CI/CD**: Настройте автоматический деплой через GitHub Actions
2. **Логирование**: Настройте централизованное логирование (например, через Logstash)
3. **Масштабирование**: Используйте балансировщик нагрузки для горизонтального масштабирования
4. **CDN**: Настройте CDN для Frontend статики

---

**Дата создания**: 2025-01-XX  
**Версия**: 1.0

