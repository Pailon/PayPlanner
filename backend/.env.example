# =============================================================================
# PayPlanner Backend Environment Configuration
# =============================================================================
# РЎРєРѕРїРёСЂСѓР№С‚Рµ СЌС‚РѕС‚ С„Р°Р№Р» РІ .env Рё Р·Р°РїРѕР»РЅРёС‚Рµ Р·РЅР°С‡РµРЅРёСЏ
# =============================================================================

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
NODE_ENV=development
PORT=3000

# -----------------------------------------------------------------------------
# Telegram Bot
# -----------------------------------------------------------------------------
# РџРѕР»СѓС‡РёС‚Рµ С‚РѕРєРµРЅ Сѓ @BotFather: https://t.me/BotFather
TELEGRAM_BOT_TOKEN=your_bot_token_here

# URL С„СЂРѕРЅС‚РµРЅРґР° РґР»СЏ Telegram Mini App (РѕР±СЏР·Р°С‚РµР»СЊРЅРѕ HTTPS)
# РСЃРїРѕР»СЊР·СѓР№С‚Рµ Cloudflare Tunnel URL
WEB_APP_URL=https://your-cloudflare-tunnel-url.com

# -----------------------------------------------------------------------------
# Security
# -----------------------------------------------------------------------------
# РЎРіРµРЅРµСЂРёСЂСѓР№С‚Рµ РЅРѕРІС‹Рµ РєР»СЋС‡Рё:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

JWT_SECRET=your_jwt_secret_here_min_32_chars
ENCRYPTION_KEY=your_encryption_key_here_64_hex_chars

# -----------------------------------------------------------------------------
# PostgreSQL Database
# -----------------------------------------------------------------------------
# Backend РїРѕРґРєР»СЋС‡Р°РµС‚СЃСЏ Рє PostgreSQL РїРѕ localhost (Docker РёР»Рё Р»РѕРєР°Р»СЊРЅС‹Р№)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=payplanner

# -----------------------------------------------------------------------------
# Redis
# -----------------------------------------------------------------------------
# Backend РїРѕРґРєР»СЋС‡Р°РµС‚СЃСЏ Рє Redis РїРѕ localhost (Docker РёР»Рё Р»РѕРєР°Р»СЊРЅС‹Р№)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# -----------------------------------------------------------------------------
# CORS
# -----------------------------------------------------------------------------
# Р Р°Р·СЂРµС€РµРЅРЅС‹Рµ РґРѕРјРµРЅС‹ (С‡РµСЂРµР· Р·Р°РїСЏС‚СѓСЋ)
# Р’РєР»СЋС‡РёС‚Рµ localhost РґР»СЏ СЂР°Р·СЂР°Р±РѕС‚РєРё Рё Cloudflare Tunnel URL РґР»СЏ Telegram
ALLOWED_ORIGINS=http://localhost:3001,https://your-cloudflare-tunnel-url.com

# =============================================================================
# РџСЂРёРјРµСЂС‹ РґР»СЏ СЂР°Р·РЅС‹С… РѕРєСЂСѓР¶РµРЅРёР№
# =============================================================================

# --- Р›РѕРєР°Р»СЊРЅР°СЏ СЂР°Р·СЂР°Р±РѕС‚РєР° (Windows) ---
# NODE_ENV=development
# WEB_APP_URL=http://localhost:3001
# POSTGRES_PASSWORD=postgres
# REDIS_PASSWORD=
# ALLOWED_ORIGINS=http://localhost:3001,http://localhost:3000

# --- Raspberry Pi СЃ Cloudflare Tunnel ---
# NODE_ENV=production
# WEB_APP_URL=https://payplanner.your-domain.com
# POSTGRES_PASSWORD=strong_password
# REDIS_PASSWORD=redis_password
# ALLOWED_ORIGINS=https://payplanner.your-domain.com

# РџСЂРёРјРµС‡Р°РЅРёРµ:
# - POSTGRES_HOST Рё REDIS_HOST РІСЃРµРіРґР° localhost (Docker РєРѕРЅС‚РµР№РЅРµСЂС‹ РЅР° С‚РѕР№ Р¶Рµ РјР°С€РёРЅРµ)
# - WEB_APP_URL РґРѕР»Р¶РµРЅ Р±С‹С‚СЊ HTTPS РґР»СЏ Telegram Mini App (С‡РµСЂРµР· Cloudflare Tunnel)
# - Cloudflare Tunnel СЂР°Р±РѕС‚Р°РµС‚ РєР°Рє РѕР±СЂР°С‚РЅС‹Р№ РїСЂРѕРєСЃРё, РїСЂРѕРєРёРґС‹РІР°СЏ С‚СЂР°С„РёРє РЅР° localhost:3000
